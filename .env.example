# =========================
# TopstepX / ProjectX API
# =========================
API_BASE=https://api.topstepx.com
TOPSTEP_USER=your_email_or_username_here
TOPSTEP_API_KEY=your_api_key_here

# Request behavior (used by shared/client.py)
REQUEST_TIMEOUT_SECONDS=30
RETRY_BACKOFF=0.5
RETRY_MAX=3
LOG_LEVEL=INFO

# =========================
# Service Ports (two entries)
# NOTE: analytics/main.py and data/main.py each read HOST/PORT from the environment.
# Since both use the same variable names, set these *per process* when you run them.
# These values document your defaults.
# =========================
# Data service
DATA_HOST=0.0.0.0
DATA_PORT=8090

# Analytics service
ANALYTICS_HOST=0.0.0.0
ANALYTICS_PORT=8091

# The analytics service needs to call the data service:
# When running locally without Docker, use localhost and DATA_PORT
DATA_API_BASE=http://localhost:8090

# Optional: controls weekend auto-window logic in analytics
AUTO_LIVE_FRESH_MINUTES=10

# Gateway / Caddy (reverse proxy)
CADDY_PORT=8080
